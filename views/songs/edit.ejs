<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header>
      <h1>Edit <%=song.title%>'s Info</h1>
      <nav>
        <ul>
          <li>
            <a href="/">Home</a>
          </li>
          <li>
            <a href="/songs">Songs Index</a>
          </li>
        </ul>
      </nav>
    </header>
    <main>
      <h2>Songs:</h2>
      <form action="/songs/<%=song._id%>?_method=PUT" method="post">
        <select name="singerId">
          <% for(let i =0; i < singers.length; i++){%>

            <option value="<%=singers[i]._id%>"

              <%if(singers[i].id.toString() === songSingers._id.toString()){ %>

                  selected

              <%}%>
              ><%=singers[i].name%></option>
          <%}%>
        </select>
        <input type="text" name="title" value="<%=song.title%>"/><br/>
        <textarea name="lyrics"><%=song.lyrics%></textarea><br/>
        <input type="submit" value="Update Song"/>
      </form>
    </main>
  </body>
</html>